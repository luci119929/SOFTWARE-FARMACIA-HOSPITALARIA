{
  "name": "@medline/e2e",
  "version": "0.1.0",
  "private": true,
  "description": "End-to-end tests for MedLine (Playwright), covering RBAC navigation and the critical purchase-order / returns flows.",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
